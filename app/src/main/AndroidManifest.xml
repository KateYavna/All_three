<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.allthree">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AllThree">
        <activity android:name=".AuthorActivity"></activity>
        <activity android:name=".EmotionsActivity" />
        <activity android:name=".Sea_Pictures_Activity" />
        <activity android:name=".QuotesActivity" />
        <activity android:name=".SignActivity" />
        <activity android:name=".AnecdotesActivity" />
        <activity android:name=".PicturesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>